<UserControl x:Class="TT_PPID_CS.UI.Views.UserManagementView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:md="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Toolbar -->
        <Border Grid.Row="0" Padding="10" BorderThickness="0,0,0,1" BorderBrush="#EEEEEE">
            <DockPanel LastChildFill="False">
                <StackPanel Orientation="Horizontal" DockPanel.Dock="Left">
                     <md:PackIcon Kind="AccountGroup" Width="24" Height="24" VerticalAlignment="Center" Margin="0,0,10,0"/>
                     <TextBlock Text="用户管理" VerticalAlignment="Center" FontSize="16" FontWeight="Bold"/>
                </StackPanel>

                <!-- Actions -->
                <StackPanel Orientation="Horizontal" DockPanel.Dock="Right">
                    <Button Content="刷新" Command="{Binding LoadDataCommand}" Style="{StaticResource MaterialDesignOutlinedButton}" Margin="5,0"/>
                    <Button Content="添加用户" Command="{Binding AddCommand}" Style="{StaticResource MaterialDesignRaisedButton}" Margin="5,0"/>
                    <Button Content="编辑" Command="{Binding EditCommand}" Style="{StaticResource MaterialDesignOutlinedButton}" Margin="5,0"/>
                    <Button Content="删除" Command="{Binding DeleteCommand}" Style="{StaticResource MaterialDesignOutlinedButton}" Margin="5,0" Foreground="Red" BorderBrush="Red"/>
                </StackPanel>
            </DockPanel>
        </Border>

        <!-- DataGrid -->
        <DataGrid Grid.Row="1" ItemsSource="{Binding Users}"
                  SelectedItem="{Binding SelectedUser}"
                  AutoGenerateColumns="False"
                  CanUserAddRows="False"
                  IsReadOnly="True"
                  SelectionMode="Single"
                  Style="{StaticResource MaterialDesignDataGrid}">
            <DataGrid.Columns>
                <DataGridTextColumn Header="ID" Binding="{Binding Id}" Width="60"/>
                <DataGridTextColumn Header="用户名" Binding="{Binding UserName}" Width="150"/>
                <DataGridTextColumn Header="全名" Binding="{Binding FullName}" Width="150"/>
                <DataGridTextColumn Header="邮箱" Binding="{Binding Email}" Width="200"/>
                <DataGridTextColumn Header="角色" Binding="{Binding Role}" Width="100"/>
                <DataGridCheckBoxColumn Header="启用" Binding="{Binding IsActive}" Width="80"/>
                <DataGridTextColumn Header="最后登录" Binding="{Binding LastLoginTime, StringFormat=\{0:yyyy-MM-dd HH:mm\}}" Width="150"/>
            </DataGrid.Columns>
        </DataGrid>

        <!-- Status Bar -->
        <StatusBar Grid.Row="2">
             <StatusBarItem>
                <TextBlock Text="{Binding Users.Count, StringFormat='共 {0} 个用户'}"/>
            </StatusBarItem>
            <StatusBarItem HorizontalAlignment="Right">
                <ProgressBar IsIndeterminate="True" Width="100" Height="10" Visibility="{Binding IsBusy, Converter={StaticResource BooleanToVisibilityConverter}}"/>
            </StatusBarItem>
        </StatusBar>
    </Grid>
</UserControl>
