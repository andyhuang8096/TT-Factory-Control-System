<UserControl x:Class="TT_PPID_CS.UI.Views.StatisticsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:md="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Border Padding="20">
            <StackPanel Orientation="Horizontal">
                <md:PackIcon Kind="GoogleAnalytics" Width="30" Height="30" VerticalAlignment="Center" Margin="0,0,10,0"/>
                <TextBlock Text="系统统计概览" FontSize="24" FontWeight="Bold"/>
                <Button Command="{Binding LoadDataCommand}" Style="{StaticResource MaterialDesignIconButton}" Margin="10,0">
                    <md:PackIcon Kind="Refresh"/>
                </Button>
            </StackPanel>
        </Border>

        <ProgressBar Grid.Row="0" IsIndeterminate="True" Height="4" VerticalAlignment="Bottom"
                     Visibility="{Binding IsBusy, Converter={StaticResource BooleanToVisibilityConverter}}"/>

        <!-- Cards -->
        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
            <WrapPanel Margin="10">
                <!-- PPID Stats -->
                <md:Card Width="200" Padding="15" Margin="10">
                    <StackPanel>
                        <TextBlock Text="总 PPID 记录" Foreground="Gray"/>
                        <TextBlock Text="{Binding Stats.TotalPPIDCount}" FontSize="32" FontWeight="Bold"/>
                        <md:PackIcon Kind="Database" HorizontalAlignment="Right" Width="24" Height="24" Opacity="0.2"/>
                    </StackPanel>
                </md:Card>

                <md:Card Width="200" Padding="15" Margin="10" Background="#E8F5E9">
                    <StackPanel>
                        <TextBlock Text="可用 (Available)" Foreground="#2E7D32"/>
                        <TextBlock Text="{Binding Stats.AvailableCount}" FontSize="32" FontWeight="Bold" Foreground="#2E7D32"/>
                    </StackPanel>
                </md:Card>

                <md:Card Width="200" Padding="15" Margin="10" Background="#E3F2FD">
                    <StackPanel>
                        <TextBlock Text="已用 (In Use)" Foreground="#1565C0"/>
                        <TextBlock Text="{Binding Stats.InUseCount}" FontSize="32" FontWeight="Bold" Foreground="#1565C0"/>
                    </StackPanel>
                </md:Card>

                <md:Card Width="200" Padding="15" Margin="10" Background="#FFEBEE">
                    <StackPanel>
                        <TextBlock Text="损坏 (Corrupted)" Foreground="#C62828"/>
                        <TextBlock Text="{Binding Stats.CorruptedCount}" FontSize="32" FontWeight="Bold" Foreground="#C62828"/>
                    </StackPanel>
                </md:Card>
                
                <!-- System Stats -->
                <md:Card Width="200" Padding="15" Margin="10">
                    <StackPanel>
                        <TextBlock Text="注册用户" Foreground="Gray"/>
                        <TextBlock Text="{Binding Stats.TotalUserCount}" FontSize="32" FontWeight="Bold"/>
                        <md:PackIcon Kind="AccountMultiple" HorizontalAlignment="Right" Width="24" Height="24" Opacity="0.2"/>
                    </StackPanel>
                </md:Card>

                <md:Card Width="200" Padding="15" Margin="10">
                    <StackPanel>
                        <TextBlock Text="导入记录" Foreground="Gray"/>
                        <TextBlock Text="{Binding Stats.ImportCount}" FontSize="32" FontWeight="Bold"/>
                        <md:PackIcon Kind="FileImport" HorizontalAlignment="Right" Width="24" Height="24" Opacity="0.2"/>
                    </StackPanel>
                </md:Card>
                
                <md:Card Width="200" Padding="15" Margin="10">
                    <StackPanel>
                        <TextBlock Text="备份记录" Foreground="Gray"/>
                        <TextBlock Text="{Binding Stats.BackupCount}" FontSize="32" FontWeight="Bold"/>
                        <md:PackIcon Kind="BackupRestore" HorizontalAlignment="Right" Width="24" Height="24" Opacity="0.2"/>
                    </StackPanel>
                </md:Card>
            </WrapPanel>
        </ScrollViewer>
        
        <!-- Charts or Details placeholder -->
        <Grid Grid.Row="2" Margin="20">
            <TextBlock Text="模型分布 (Model Distribution)" Margin="0,0,0,10" FontWeight="Bold"/>
            <ItemsControl ItemsSource="{Binding Stats.ModelDistribution}" Margin="0,20,0,0">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Grid Margin="0,5">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="150"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="50"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Text="{Binding Key}"/>
                            <ProgressBar Grid.Column="1" Value="{Binding Value}" Maximum="{Binding DataContext.Stats.TotalPPIDCount, RelativeSource={RelativeSource AncestorType=UserControl}}" Height="10"/>
                            <TextBlock Grid.Column="2" Text="{Binding Value}" Margin="10,0,0,0"/>
                        </Grid>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </Grid>
    </Grid>
</UserControl>
